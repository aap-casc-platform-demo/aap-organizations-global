# aap-organizations-global

Platform-level CasC repo for AAP organization definitions. Also serves as the **registry repo** containing `repos-manifest.yml`.

Managed by the platform team. Updated automatically by the tenant bootstrap playbook.
